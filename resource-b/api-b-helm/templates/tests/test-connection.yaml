apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-b-helm.fullname" . }}-test-connection"
  labels:
    {{- include "api-b-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-b-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
